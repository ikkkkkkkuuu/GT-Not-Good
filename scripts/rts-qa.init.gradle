// Focused regression tests for the embedded official RTS integration.
gradle.projectsEvaluated {
    gradle.rootProject.tasks.named('compileTestJava').configure {
        setSource(gradle.rootProject.fileTree('src/test/java/com/xyp/gtnotgood/client/rts') { include 'OfficialRts*.java' })
    }
}
