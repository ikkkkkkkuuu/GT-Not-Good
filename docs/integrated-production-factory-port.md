## 大批量输出与样板总成

修复产线缓冲输出的固定小包限制：原来每种物品每 tick 仅输出一组、每种流体仅输出 64,000 L。现在使用 GT ItemEjectionHelper/FluidEjectionHelper 的事务接口提交完整待输出数量，接入 PatternMEOutputMultiblockMixin 暴露的样板总成输出适配器。允许多个输出端部分接收，仅在提交后更新剩余量，开启保护避免虚空；每 tick 仍限制处理 32 种产物。

缓存阈值不再计算 Integer.MAX_VALUE 减整组节点并行输出量，避免大节点并行产生负阈值而阻止较小批量补充；真正可执行的输出容量仍由 FactoryBatching.recipeLimit 检查。某个上游等待下游但其他节点仍执行时，不再把整机状态覆盖成等待下游。

编译与 52 项 Factory 测试通过，新增缓存阈值溢出回归测试。已核对自定义样板总成的事务输出适配接口，未在游戏内复现或验证用户的具体产线。

## 自动执行并行与基础预览分离

移除主界面的整机并行倍率输入框、同步值和修改命令，同时隐藏电源面板的并行配置行。主机器声明最大并行为 Integer.MAX_VALUE。旧存档中的 factoryGlobalParallel 不再参与计算，已开始的任务继续使用已保存批量。

FactoryPreview.describe 及按图导出的 FactoryPatternExport.create 不再接收整机倍率。预览的输入、输出、耗电和免费 AE 样板只由节点并行、配方、超频和连接关系决定；没有整机 ×N 标签。节点并行、自动配平、工序整体翻倍/减半仍能改变基础规格。

执行器按节点独立调度，普通节点从可用输入开始，以剩余每 tick 能源、最坏情况输出数量和当前内部缓存空间收紧批量；足够时取节点并行的整数倍，不足一个节点批量时仍可执行较小批量。循环按共同周期的基础计划整体同比放大，并检查各成员完整输出的整数容量及净输入。最大 int 是上限，不是无条件执行数量。预览是基础规格，不承诺自动运行时的实时吞吐量。保留每个样板输入总成缓存独立匹配。

编译、资源生成与 51 项 Factory 测试通过；新增覆盖最大整数容量、重复输出总量、剩余能源、节点批量对齐和边界不溢出。游戏内主界面、自动批量及 AE 样板读取仍待验证。

## 主机器产线耗时与进度

主机器运行时间不再固定为 1 tick，也不新增固定 20 tick 配置。先计算各节点超频后耗时，再将循环强连通分量压缩为执行器使用的共同周期；串联相加，分支取最长路径。并行只增加批量，不缩短节点耗时。

FactoryProgress 根据实际支付能源并推进的节点任务记录已完成工作，防止上游补料重启使整线进度倒退。所有阶段完成后，在后续任务运行时开始下一轮记录；暂停、缺电或没有任务时不会凭空增加进度。记录随 NBT 保存，切换已安装工序时重建。保持原有连续流水生产，主进度表示整线一轮工作完成度，不是某一件产物的独立批次追踪；缺料和堵塞可能使实际等待时间超过计算耗时。超过 GT 整数进度接口上限时，总值截到 Integer.MAX_VALUE，进度同比缩放。失效配方不影响旧任务排空退还。

编译与 46 项 Factory 测试通过，新增测试覆盖串联超频、分支汇合、循环周期、补料与存档、空闲和长整数耗时。原测试随工具类迁移到 utils.machine.factory 包以恢复内部 API 访问。游戏内主界面和 Waila 进度仍待验证。

## 电力高炉与 DTPF 温度

按用户明确要求，blastFurnaceRecipes 和 plasmaForgeRecipes 的正温度值与 COIL_HEAT 元数据可导入，产线执行不检查线圈温度，不附加原机器的余热折扣或其他机器专属加成。其他配方图的特殊值、环境条件和特殊物品仍按原规则检查。普通配方身份不变，新支持的温度配方指纹包含特殊值。

## 预览单位调整

预览、悬浮提示和免费 AE 样板统一采用每 tick 净输入输出。速率公式为“配方数量 × 有效并行 ÷ 耗时 tick”，不再乘20。已有编码样板保持原内容，需要重新导出才使用新数量；小数仍整组同比放大成整数。

## 状态与退还界面优化

主界面分别提示物品/流体输出堵塞、缺电、缺少控制器或不可消耗物，以及退还受阻。清空工序后右侧显示实际尚未退还的收纳物清单、待完成工序数和已完成但未排出的物品/流体数量；暂停和缺电时进度信息仍可查看。待退还列表随部分退还实时减少，悬浮提示跟随当前行更新。

37项 Factory 测试、编译和资源生成通过；客户端布局与真实多总线退还仍待游戏内验证。

## AE 样板导出（当前行为）

锁定工序并完成安装后，列表右侧可点击“导出AE样板”。按用户要求免费生成，直接放入输出总线，不消耗空白样板。输出总线拒收时提示腾出空间。

输入输出与 FactoryPreview 使用同一个净流量模型并计入当前并行。任一侧含流体使用 AE2 rv3-beta-1050 的 encodedUltimatePattern 和 writeToNBTGeneric；纯物品使用 encodedPattern 与物品 NBT，并标为加工配方。没有旧版流体包编码。非整数数量整体同比放大成整数；超出编码容量时拒绝生成。

预览保留理论输入输出；黄色感叹号标出内部保留余量，概率产线显示期望值警告。存在内部余量或概率产出时，服务端拒绝导出确定产出的 AE 样板并给出原因；确定产出的平衡产线仍免费导出。空净输入或空净输出无法生成普通加工样板。

编译、资源生成及34项 Factory 测试通过，覆盖流体出现在任意一侧的类型选择、两种NBT写入路径和数量整数化；尚未在游戏中验证AE终端读取和实际合成。

## 并行与配平更新

默认工序并行及整线倍率均为 1。可配置上限改为 BOX 无限模式所用的 Integer.MAX_VALUE（2,147,483,647）。两者乘积以 long 计算，并把有效并行限制在 GT 的整数并行范围内；物品、流体和耗电仍受底层数值容量限制，溢出批次不扣原料。

自动配平填写各工序的并行比例，不改电压、超频和整线倍率；多个生产者和重复输出槽按材料汇总。解算上限不再是64，但仍有服务器计算时间预算，比例冲突或搜索受限会保留原并行设置。大并行概率产物使用 GT 自带的大批量概率计算。

## 2026-09-10：界面方向改为 BOX 工序列表

用户明确要求保留当前主机器，GUI 和交互以同级 BoxPlusPlus-master 为参考。以下是当前实现状态；后面的 SuperFactory 图编辑说明为早期实现记录。

- 工序入口改为滚动列表；列表页通过 NEI 的 + 直接追加已注册配方。详情页查看输入、输出、不可消耗物并编辑并行、超频和电压。
- 自动按 GT 物品匹配和流体身份生成材料联系，不再手动建空节点或连线。
- 提供整组 ×2、÷2 和自动配平；超出并行上限或无法整数减半时整组不变。
- 汇总预览保留用户指定的左输入、右输出图标和数量。确认后服务器锁定工序；预览期间数据发生变化时拒绝确认并要求重新查看。
- 锁定后仍可查看，清空经过确认；运行中的任务完成并退还收纳物后卸载。原有结构、能源仓优先/无能源仓无线、催化物收纳及无启动物料循环保持原执行后端。
- 导入导出采用 GTNG1 文本，只传递本地配方身份与工序配置。服务器完整校验后才替换空列表，不能导入任意输入/输出 NBT。
- 未完成的 BOX 对齐项：AE 样板导出、BOX 原生 NBT 分享码兼容、数字验证清空流程；BOX 的实体环与模块系统未迁移。特殊配方支持仍由现有适配范围决定。
- 参考来源：BoxPlusPlus 的 GTMachineBox#createInitialingWindow/createRoutingWindow/createFinalRecipeWindow 与 BoxRoutings#makeRouting。MUI2 代码独立实现，未直接复制 BOX 的 MUI1 源码或资源。
- 验证：compileJava、processResources、25 项 Factory 单元测试通过。尚未在运行客户端验证布局、滚动、NEI 往返与多人同步。

# 产线集成工厂：源码对照与移植状态

对照目录：同级 `SuperFactory-master`。此实现尚不是原版的完整移植。

## 本次修正

- 原版 `MTESuperIntegratedFactory.machineSupportsRecipeMap` 接受实现 `RecipeMapWorkable`、支持对应配方表的多方块控制器。本项目已移除单方块机器及其电压等级限制。
- 原版 `processInputMode` 收入控制器和不可消耗物，保存在 `ProcessRequirements`，在输出模式返还。本项目改为每节点独立保管真实物品及 NBT；所有节点需求齐全才启动产线。
- 每个普通节点需要一个控制器，每个不可消耗输入槽需要一个实际物品。控制器、模具、电路等保管物不进入生产原料池，也不按并行数重复收入。总线电路配置槽不视为实际物品。
- 保存世界时记录保管物；卸载产线或替换已提交工序时先完成任务、排出物料、返还保管物。输出堵塞时保留尚未返还的物品。拆除控制器时保管物掉落。
- 节点详情页底部显示当前运行产线保管的真实物品；上面的配方图标仍是需求说明。
- 增加自动配平按钮：按连线、耗时（含当前超频）、物品/流体数量与物品概率产出构造吞吐方程，求正整数并行比例。只修改草稿，须提交工序后应用。
- 配平采用精确分数。保留当前每节点 64 并行上限，并限制求解时间与自由变量搜索规模。无解、超出限制或搜索未找到方案时不改动草稿。概率配方平衡的是期望产量，不能保证每批恰好相等。
- 任务完成时立即尝试后续生产，避免短配方被原先每 10 tick 的缺料重试间隔限制吞吐。

## 已有基础功能

节点详情可将配方节点设为目标产物节点，画布以金色边框和星号标记；目标标记随草稿与存档保存。提交及基础配平检查每个独立连通分量至少有一个目标节点。既有运行产线不会因更新而被清除，新提交需要设置目标。

工序 GUI 的“产线预览”查看当前已同步草稿的理论满并行速率，含整机倍率、超频、物品概率产量与总 EU/t。预览利用实际连线及配方输入匹配计算内部供需，列出外部输入、对外输出和留在内部的剩余物料；不可消耗物不计入原料速率。预览不会读取或消耗总线库存，也不代表实时产量。按用户要求，闭环不需要物理启动物料；目标产量反推仍未实现。

MUI2 节点画布、节点详情、NEI 配方“＋”导入、拖动和连线、并行/超频设置、草稿提交、产线卸载、内部物料缓冲、有能源仓时普通供电／无能源仓时无线供电。

节点详情现可编辑基础 EU/t，并预览单并行超频后的耗电和耗时；数值随草稿提交，恢复配方默认可撤销覆盖。原版这里编辑的是 EU/t，不是单独的电压等级选择器。

主界面提供整机并行倍率（1–64），实际节点并行上限为节点并行乘以整机倍率，最高 4096。倍率保存到机器，下一个启动批次使用新倍率，已经启动的批次保留原数值；保管物需求不随倍率增加。

## 仍未移植或存在差异

| 原版能力 | 当前情况 |
| --- | --- |
| 目标配平与目标产物、复杂副产物和循环的专门分析 | 尚未移植；当前是严格吞吐方程配平 |
| 复合节点、回收节点、代理工厂控制器 | 尚未移植 |
| 工序文件导入导出、原材料清单导出 | 尚未移植 |
| 恢复已提交图、视图复位、批量重置并行/节点状态 | 尚未移植 |
| 广泛的特殊机器和虚拟 NEI 配方适配 | 尚未移植；当前遍历全部已注册 GT 配方表（含蒸馏室、蒸馏塔），仍需适配特殊条件、元数据、虚拟配方和自定义处理逻辑 |
| 原版启动资源收集和专门输入/运行/输出管理页面 | 当前保留按节点调度与安全排空流程，尚未完整移植原版页面和启动资源分析 |
| 原版高并行和其他性能配置 | 当前最多 32 节点、每节点基础并行 64、整机倍率 64；未移植全局额外超频等其他参数 |

## 验证边界

自动化检查覆盖编译、语言资源生成、吞吐比例求解、失败时不产生结果、保管物只收入一次、不同节点分别收入、输出失败后保留及避免重复返还、写入物品 NBT，以及既有生产状态测试。

仍需重启开发客户端实际验证：NEI 导入、多方块控制器收入、催化物收入、清空输入总线后持续运行、卸载返还、保存重进后的真实物品恢复、界面显示与点击。普通 JUnit 环境没有 Forge 启动类加载器，不能替代这些验证。

配方导入已取消原先 15 类配方表白名单。NEI 导入失败提示现在区分虚拟配方、特殊条件、元数据、槽位上限、概率输入/流体、自定义实现、非 GT 处理器及注册配方未匹配，并显示对应配方表标识。配方表全收录不等于所有特殊配方执行逻辑已经适配。

大型化学反应釜由 UniversalChemical 自动写入 CLEANROOM=false，这表示不要求无尘室；该标记及 LOW_GRAVITY=false 已作为关闭的环境条件放行，不再因元数据非空而拒绝导入。真正启用的环境条件及其他未适配元数据仍单独拦截。


## 后续修正：闭环与图标预览

- 按用户要求，闭环无需先放入启动物料。强连通节点组以配方耗时的最小公倍数作为共同结算周期，按节点并行和整机倍率执行整次数配方，沿实际连线抵扣内部物料。只消耗净外部输入并产出净剩余量，不创建可以被卸载取走的虚构启动物料。
- 净输入、供电与输出容量检查全部通过后，闭环各节点批次一起启动；共同周期付电完成后才释放输出。运行批次沿用现有存档、暂停和排空机制。共同周期或物料数量溢出、单节点共同周期执行次数超过 65536 时拒绝启动并提示数值限制。
- 预览改为左侧输入、右侧输出的独立图标网格，各自分页。物品和流体显示图标，右下角显示每秒数量，悬停显示完整名称、数值及是否属于内部剩余；按真实物品 NBT/流体身份合并数量。
- BoxPlusPlus-master 已获用户授权参考；本次图标控件直接使用当前项目的 MUI2 物品显示与数字绘制接口，未复制其旧版 GUI。
- 自动化验证包括闭环分组、整次数共同周期、数值溢出拒绝、连线流量抵扣与净缺口。真实配方注册环境中的闭环整批执行以及新预览视觉效果仍待客户端验证。
